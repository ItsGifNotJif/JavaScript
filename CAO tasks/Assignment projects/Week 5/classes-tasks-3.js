console.groupCollapsed('https://edabit.com/challenge/2FF7RKw9RLwc3EBY9');
{
  class Challenge{
    constructor(id, level){
      this.id = id;
      this.level = level;
    }

    points(){
      if(this.level = "VE") return 5;
      if(this.level = "EA") return 10;
      if(this.level = "ME") return 20;
      if(this.level = "HA") return 40;
      if(this.level = "VH") return 80;
      if(this.level = "EX") return 120;
    }
  }

  class User{
    constructor(name, xp, log){
      this.name = name;
      this.xp = xp;
      this.log = log;
    }
  }

  newSolvedChallenge()
}
console.groupEnd();

console.groupCollapsed('https://edabit.com/challenge/ifDM26p25bqS8EsFu');
{
  // ... code
}
console.groupEnd();
